import React from 'react';

function AddEmployee() {
    return (
        <div>
            <h1>Add Employee</h1>
            {/* Add a form to allow the user to add a new employee */}
        </div>
    );
}

export default AddEmployee;